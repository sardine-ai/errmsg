version: 2
jobs:
  build:
    docker:
      - image: cimg/go:1.13

    working_directory: /go/src/github.com/daisy1754/errmsg
    steps:
      - checkout
      - run: go get -v -t -d ./...
      - run: go test -v ./...
